import React from 'react';

export type IconName =
  <% icons.forEach(icon => { -%>
    | '<%- stripFileExtension(icon) %>'
  <% }) %>;

export type Icons = Record<IconName, React.FC<React.SVGProps<SVGSVGElement>>>;