# Migration `20200128133341-init`

This migration has been generated by Maxwell Brown at 1/28/2020, 1:33:41 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."Form" (
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "id" TEXT NOT NULL  ,
    "owner" TEXT NOT NULL  ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY ("id"),FOREIGN KEY ("owner") REFERENCES "User"("id") ON DELETE RESTRICT
) 

CREATE TABLE "quaint"."FormField" (
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "form" TEXT NOT NULL  ,
    "id" TEXT NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    "type" TEXT NOT NULL DEFAULT 'Button' ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY ("id"),FOREIGN KEY ("form") REFERENCES "Form"("id") ON DELETE RESTRICT
) 

CREATE TABLE "quaint"."User" (
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "email" TEXT NOT NULL DEFAULT '' ,
    "firstName" TEXT NOT NULL DEFAULT '' ,
    "id" TEXT NOT NULL  ,
    "lastName" TEXT NOT NULL DEFAULT '' ,
    "password" TEXT NOT NULL DEFAULT '' ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200128133341-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,61 @@
+datasource db {
+  provider = "sqlite"
+  url      = "sqlite:./dev.db"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Form {
+  id        String      @id @default(cuid())
+  fields    FormField[]
+  owner     User
+  createdAt DateTime    @default(now())
+  updatedAt DateTime    @updatedAt
+}
+
+model FormField {
+  id        String    @id @default(cuid())
+  name      String
+  type      InputType
+  form      Form
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+}
+
+model User {
+  id        String   @id @default(cuid())
+  firstName String
+  lastName  String
+  email     String   @unique
+  password  String
+  forms     Form[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+enum InputType {
+  Button
+  Checkbox
+  Color
+  Date
+  DateTimeLocal
+  Email
+  File
+  Hidden
+  Image
+  Month
+  Number
+  Password
+  Radio
+  Range
+  Reset
+  Search
+  Submit
+  Tel
+  Text
+  Time
+  Url
+  Week
+}
```


