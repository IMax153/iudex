{
  "name": "@iudex/api",
  "version": "0.1.0",
  "description": "The API server for Iudex.",
  "main": "dist/index.js",
  "author": "Maxwell Brown <maxwellbrown1990@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist && rimraf prisma/migrations && rimraf prisma/dev.db",
    "dev": "dotenv -e .env.development -- ts-node-dev --no-notify --respawn --transpileOnly src",
    "generate": "yarn generate:prisma && yarn generate:nexus",
    "generate:nexus": "ts-node --transpile-only src/schema",
    "generate:prisma": "prisma2 generate",
    "migrate:save": "prisma2 migrate save",
    "migrate:up": "prisma2 migrate up",
    "migrate:down": "prisma2 migrate down",
    "seed": "ts-node prisma/seeds",
    "precommit": "lint-staged",
    "postinstall": "yarn generate"
  },
  "dependencies": {
    "@prisma/client": "^2.0.0-preview020.2",
    "apollo-server-express": "^2.9.16",
    "bcryptjs": "^2.4.3",
    "express": "^4.17.1",
    "graphql": "^14.6.0",
    "nexus": "^0.12.0-rc.9",
    "nexus-prisma": "^0.7.0-next.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/express": "^4.17.2",
    "@types/faker": "^4.1.9",
    "@types/node": "^13.5.0",
    "dotenv-cli": "^3.1.0",
    "faker": "^4.1.0",
    "ts-node": "^8.6.2",
    "ts-node-dev": "^1.0.0-pre.44"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix"
    ]
  }
}
